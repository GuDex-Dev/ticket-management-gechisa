export const metadata = {
  title: "Iniciar Sesión - Gechisa - ADMIN",
  description: "WebApp de gestión de boletos para la empresa Gechisa",
};

export default function LoginLayout({ children }) {
  return <>{children}</>;
}
